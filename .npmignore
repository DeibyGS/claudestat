# INACTIVO — package.json `files` array controla qué se incluye en el tarball.
# Este archivo se mantiene solo para documentación de exclusión legacy.
# Para modificar el contenido publicado, editar `files` en package.json.
